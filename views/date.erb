<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Date</title>
  </head>
  <body>
    <% require 'date' %>
    <% date = DateTime.now %>
    <h1>Today
    <p>The date is <%= date.strftime("%A")%>, <%= date.strftime("%B") %> <%= date.strftime("%d") %>, <%= date.strftime("%Y") %></p>
  </body>
</html>
